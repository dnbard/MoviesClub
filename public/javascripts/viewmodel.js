function Viewmodel(model){
    this.movies = ko.observableArray([]);
    this.movies.push({});
    this.movies.push({});
    this.movies.push({});
    this.movies.push({});
    this.movies.push({});
}
